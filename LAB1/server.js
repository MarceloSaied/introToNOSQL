var app = require('./app'); // imports app.js
app.listen(8080, function() { // starts listening on http://localhost:3000/
	console.log('listening on port 8080'); // print notification on the console
});
